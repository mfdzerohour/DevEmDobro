const boletoPago = false;

// if (boletoPago === true) { quando é verdadeiro não precisa do true
if (boletoPago) {
    alert('O boleto está pago!')
} else if (boletoPago === false) {
    alert('O boleto não está pago!')
}