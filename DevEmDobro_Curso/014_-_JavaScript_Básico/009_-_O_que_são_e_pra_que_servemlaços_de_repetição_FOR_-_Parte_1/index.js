/*
inicializador = varaivel que vai ser inicializada
condição = expressao que vai ser avaliada em cada interação
incremento = a cada interação ele vai incrementar a variavel inicializada

for ( inicializador; condição; incremento ) {
    bloco de códig  que vai ser rodado    
}

*/

for (let i = 1; i <= 100; i++){
    console.log("olá mundo ", i);
}